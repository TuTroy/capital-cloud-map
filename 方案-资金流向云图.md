# 🌊 资金云图 — 技术方案

> 每日A股资金热度可视化工具  
> 数据驱动，行业下钻，一眼看清钱在哪交易

---

## 一、核心目标

**一句话：** 每天打开浏览器，一眼看清今天全A股的资金流向了哪些行业、流出了哪些行业，从行业一路点到个股。

**不是投资建议，是情报工具。**

---

## 二、核心指标（共6个）

每个行业/个股展示六个维度：

| 指标 | 含义 | 用途 |
|------|------|------|
| **成交额（亿）** | 今天交易了多少金额 | 热度——钱在哪交易 |
| **涨跌幅（%）** | 价格涨还是跌 | 方向——是涨是跌 |
| **市值变化（亿）** | 今天市值 - 昨收市值 | 总价值的增减 |
| **流通市值变化（亿）** | 今天流通市值 - 昨收流通市值 | 更精准的价值变化（剔除大股东不流通部分） |
| **成交额占比（%）** | 该行业成交额 ÷ 全市场成交额 | 权重——占市场多少比例 |
| **换手率（%）** | 成交额 ÷ 流通市值 | 活跃度——是换手活跃还是死水 |

### 数据之间的逻辑关系

```
如果：
  成交额大 + 涨跌幅正 + 流通市值变化正 = ✅ 资金真在涌入
  成交额大 + 涨跌幅负 + 流通市值变化负 = ❌ 资金在出逃
  成交额大 + 涨跌幅平 + 市值变化小   = ⚠️ 多空博弈激烈
  成交额小 + 涨跌幅大                 = ⚠️ 没有大资金参与，虚涨
```

**说明：** 市值变化 ≠ 资金流入。它反映的是"价值变化的结果"，结合成交额一起看，才能判断是不是资金推动的。

---

## 三、数据分类

### 3.1 行业分类（三级下钻）

```
一级（31个） → 二级（100+） → 三级（300+）
   ↓               ↓               ↓
  银行           银行             银行
  电子  →   半导体/元件/光学  →   ...
  医药  →   ...
  ...
```

**分类来源：**
- **唯一来源：** 你手动维护的申万三级分类表（股票代码→三级行业→二级→一级）
- 匹配不到的新股直接归入"未分类"，终端打印"X只未匹配"提醒你回头补充

**计算方式：**
- 行业级数据 = 该行业下所有成分股数据的**求和**（成交额）或**加权平均**（涨跌幅）
- 权重=**成交额加权**：成交额越大的股票，对行业涨跌幅影响越大
- 首次运行，只有今日无昨日，所以"变化"字段标记为 `-`，次日开始自动计算

### 3.2 板块/指数筛选

| 筛选项 | 说明 |
|--------|------|
| **板块** | 主板 / 创业板 / 科创板 / 北交所 |
| **沪深300** | 仅看沪深300成分股汇聚的行业 |
| **中证500** | 仅看中证500成分股汇聚的行业 |
| **千亿市值以上** | 仅看市值≥1000亿的股票汇聚的行业 |

**筛选逻辑：** 筛选条件下，只取符合条件股票的数据重新汇聚行业指标。不是隐藏，是重新算。

**板块判断：** 从股票代码前缀推导：`60xxxx`=沪市主板，`00xxxx`=深市主板，`30xxxx`=创业板，`688xxx`=科创板，`83/87/43xxxx`=北交所。

**指数成分股：** 通过 baostock `query_hs300_stocks()` / `query_zz500_stocks()` 在 fetch.py 中动态拉取（非静态 CSV），每次运行自动更新。

---

## 四、可视化方案

### 4.1 页面布局

```
┌──────────────────────────────────────────────────────────┐
│  标题栏：资金云图 | 日期选择器 | 板块筛选 | 指数筛选      │
├────────────────────────────────────────┬─────────────────┤
│                                        │                 │
│       主视图（占70%宽度）               │  侧边栏排名      │
│                                        │                 │
│       Treemap 矩形树图                  │  TOP 5 流入行业  │
│                                        │  (流通市值变化↓) │
│       面积 = 成交额                     │  1. 电子 +50亿  │
│       颜色 = 涨跌幅                     │  2. 银行 +30亿  │
│       红色 = 涨  绿色 = 跌              │  3. ...         │
│       深红 = 大涨  深绿 = 大跌           │                 │
│                                        │  TOP 5 流出行业  │
│                                        │  (流通市值变化↑) │
│                                        │  1. 医药 -20亿  │
│       点击下钻：一级→二级→三级→个股      │  2. ...         │
│       双击返回上一级                     │                 │
│       顶部面包屑：首页 > 电子 > 半导体    │                 │
│                                        │                 │
│                                        │ 当前选中行业个股  │
│                                        │  成交额排名 TOP10 │
└────────────────────────────────────────┴─────────────────┘
│  底部数据表：当前层级所有条目按成交额降序                  │
│  列：行业/代码 | 名称 | 成交额 | 涨跌幅 | 市值变化       │
│      流通市值变化 | 占比 | 换手率 | 成分股数             │
└──────────────────────────────────────────────────────────┘
```

### 4.2 Treemap 视觉优化

为了让矩形树图"好看"且"直观"，采用以下设计：

| 设计点 | 方案 |
|--------|------|
| 圆角 | 矩形圆角 8px，间距 4px，清爽不挤 |
| 颜色 | 红绿渐变（深红→浅红→灰→浅绿→深绿），不刺眼 |
| 文字 | 每个格子显示**涨跌幅**（大字号）+ **成交额**（小字号） |
| 悬浮 | 鼠标悬浮展示完整六维指标 + 成分股数 |
| 动画 | 点击下钻时有平滑过渡动画 |
| 空数据 | 无数据时显示"暂无数据"灰色占位 |
| 响应式 | 自适应窗口大小，但优先占满 body |

### 4.3 交互操作

| 操作 | 效果 |
|------|------|
| 点击一级行业 | 下钻到该行业的二级分类 |
| 点击二级行业 | 下钻到该行业的三级分类 |
| 点击三级行业 | 下钻到该行业下的个股列表（Treemap变成个股） |
| 双击空白区域 | 返回上一级 |
| 面包屑导航 | 直接点击任意层级跳转 |
| 日期选择 | 切换查看历史某天的数据，仅高亮有数据的日期，无数据日期置灰 |
| 板块筛选 | 切换后重新汇聚行业数据 |
| 指数筛选 | 切换后重新汇聚行业数据 |

---

## 五、技术架构

### 5.1 目录结构

```
money_daily/
│
├── 方案-资金云图.md               ← 本方案文档
├── README.md                   ← 使用说明
├── requirements.txt            ← Python依赖
├── app.py                      ← Flask启动入口（主程序）
├── config.py                   ← 配置文件
│
├── data/
│   ├── sw_industry_map.xlsx    ← 你手动维护的申万三级分类表
│   └── history.db              ← SQLite历史数据
│
├── scripts/
│   ├── fetch.py                ← 拉取全A股代码列表(baostock) + 行情(腾讯qt.gtimg.cn)
│   ├── classify.py             ← 申万分类匹配+行业汇聚计算
│   └── update_db.py            ← 写入SQLite
│
├── templates/
│   └── index.html              ← ECharts可视化页面
│
└── static/
    └── style.css               ← 自定义样式
```

### 5.2 数据流

```
手动运行 python app.py
       │
       ▼
  检查SQLite中是否已有今日数据？
       │
       ├── 有 → 跳过拉取，直接启动Flask
       │
       └── 无 ↓
① fetch.py → baostock 获取全A股代码列表
       │
       ▼
② fetch.py → 分批调用腾讯 qt.gtimg.cn 拉取实时行情
       │    (每批50只，间隔3秒，约2-3分钟拉完全部5000+只)
       │    GBK 编码需解码，返回：当前价、昨收、涨跌幅、成交额、换手率、总市值、流通市值
       │    停牌股票（成交额=0）自动跳过
       │
       ▼
③ fetch.py → baostock 拉取沪深300/中证500成分股，标记 is_hs300 / is_zz500
       │
       ▼
④ classify.py → 读取 sw_industry_map.xlsx 做分类匹配
       ├─ 代码前缀判断板块：60=沪主板/00=深主板/30=创业板/688=科创板/8x=北交所
       ├─ 匹配成功 → 归入申万三级行业
       └─ 匹配失败 → 归入"未分类"，终端打印"X只未匹配"
       │
       ▼
⑤ classify.py → 按一级→二级→三级分别汇聚计算，侧边栏用流通市值变化排序
       │
       ▼
⑥ update_db.py → 将今日行业数据+个股数据写入 SQLite
       │
       ▼
⑦ Flask启动 → 从SQLite读取数据 → 渲染index.html
       │    首次拉取数据时终端打印进度条
       │
       ▼
⑧ 浏览器打开 http://localhost:8080
       └─ Treemap展示 + 交互操作
```

**缓存策略：** app.py 启动时先查 `industry_daily` 表是否有今日数据，有则直接启动 Flask，无需重复拉取。

**数据源说明：**
- **股票列表：** baostock `query_all_stock()` — 免费、稳定、无频率限制
- **实时行情：** 腾讯 `qt.gtimg.cn` — 免费、无需 API Key、社区公认 2025-2026 年最稳的免费行情接口
- **兜底方案：** 两个接口都是非官方 API，哪天挂了可切回 ak share（需大陆 IP）或 tushare

### 5.3 数据存储设计

#### 行业每日快照（industry_daily）

```sql
CREATE TABLE industry_daily (
    date            TEXT,     -- 日期
    level           TEXT,     -- 一级/二级/三级
    industry_code   TEXT,     -- 行业代码
    industry_name   TEXT,     -- 行业名称
    stock_count     INTEGER,  -- 成分股数量
    total_mkt_cap   REAL,    -- 总市值(亿)
    float_mkt_cap   REAL,    -- 流通市值(亿)
    turnover        REAL,    -- 成交额(亿)
    change_pct      REAL,    -- 涨跌幅(%)
    mkt_cap_change  REAL,    -- 总市值变化(亿)
    float_cap_change REAL,   -- 流通市值变化(亿)
    turnover_ratio  REAL,    -- 换手率(%)
    market_share    REAL,    -- 成交额占比(%)
    prev_turnover   REAL,    -- 昨日成交额(亿，用于对比)
    PRIMARY KEY (date, industry_code, level)
);
```

#### 个股每日快照（stock_daily）

```sql
CREATE TABLE stock_daily (
    date            TEXT,
    code            TEXT,     -- 股票代码
    name            TEXT,     -- 股票名称
    industry_l1     TEXT,     -- 一级行业
    industry_l2     TEXT,     -- 二级行业
    industry_l3     TEXT,     -- 三级行业
    board           TEXT,     -- 主板/创业板/科创板/北交所
    total_mkt_cap   REAL,    -- 总市值(亿)
    float_mkt_cap   REAL,    -- 流通市值(亿)
    turnover        REAL,    -- 成交额(亿)
    change_pct      REAL,    -- 涨跌幅(%)
    turnover_ratio  REAL,    -- 换手率(%)
    is_hs300        INTEGER, -- 1=是沪深300
    is_zz500        INTEGER, -- 1=是中证500
    PRIMARY KEY (date, code)
);
```

### 5.4 历史数据查询逻辑

| 你想知道 | 查询逻辑 |
|----------|---------|
| "某天所有行业的资金流向" | `SELECT * FROM industry_daily WHERE date='2026-05-28' AND level='一级' ORDER BY turnover DESC` |
| "某个行业过去30天的趋势" | `SELECT * FROM industry_daily WHERE industry_name='电子' AND level='一级' ORDER BY date` → 前端画折线图 |
| "最近一周累计流入最多的行业" | `SELECT industry_name, SUM(mkt_cap_change) FROM industry_daily WHERE date BETWEEN '2026-05-21' AND '2026-05-28' AND level='一级' GROUP BY industry_name ORDER BY SUM DESC` |
| "某天某个行业下的个股排名" | `SELECT * FROM stock_daily WHERE date='2026-05-28' AND industry_l1='电子' ORDER BY turnover DESC` |
| "某只股票的历史资金趋势" | `SELECT * FROM stock_daily WHERE code='000725' ORDER BY date` |

---

## 六、历史数据说明

### 6.1 数据范围

- 只存储**你手动运行过的日子**的数据
- 不是每天自动存，是每次跑脚本时才存
- 如果你连续10天没跑，中间这些天的数据就没有
- 非交易日（周末/节假日）运行会检测到无数据，提示"今日非交易日"并退出

### 6.2 首次运行

- 只有今日数据，没有昨日数据
- 市值变化、流通市值变化标记为 `null`（前端显示为 `-`）
- 次日再跑，自动载入昨日数据做对比

### 6.3 数据量估算

| 数据 | 每日条数 | 一年存储量 |
|------|---------|-----------|
| 一级行业 | 31 条 | ~7,500 条 |
| 二级行业 | ~100+ 条 | ~24,000 条 |
| 三级行业 | ~300+ 条 | ~72,000 条 |
| 个股 | ~5,000 条 | ~1,200,000 条 |
| **合计** | ~5,431 条 | ~1.3M 条 |

SQLite 处理百万级数据完全无压力。

---

## 七、运行方式

### 7.1 首次准备

```bash
cd /Users/troy_tu/.openclaw/agency-agents/stock-analyst/money_daily

# 安装依赖
pip install -r requirements.txt

# 把你维护的申万分类表放到 data/ 目录下
cp /Users/troy_tu/.openclaw/agency-agents/stock-analyst/market_daily/data/self/行业分类表/申万三级分类表.xlsx data/sw_industry_map.xlsx
```

### 7.2 每日使用

```bash
cd /Users/troy_tu/.openclaw/agency-agents/stock-analyst/money_daily
python app.py
```

会自动执行：
```
检查今日数据是否存在：
  ├─ 已有 → 直接启动Flask，秒开
  └─ 没有 ↓
① baostock 获取全A股代码列表 + 沪深300/中证500成分股
② 腾讯 qt.gtimg.cn 分批拉取行情（每批50只，约2-3分钟）
③ 申万三级行业分类匹配 + 板块判断
④ 汇聚计算行业指标（流通市值变化排序流入/流出）
⑤ 写入SQLite
⑥ 启动Flask，打开浏览器
```

同一天再次运行会跳过数据拉取，直接启动服务。

### 7.3 设置定时任务（可选）

如果你某天想让它自动跑，设个 cron：

```bash
# 每天18:30（收盘后）自动拉取数据并启动服务
30 18 * * 1-5 cd /Users/troy_tu/.openclaw/agency-agents/stock-analyst/money_daily && python app.py
```

---

## 八、依赖清单

```
flask>=3.0.0           # Web服务
requests>=2.28.0       # HTTP请求（腾讯行情接口）
baostock>=0.9.0        # A股代码列表
openpyxl>=3.1.0        # 读取xlsx申万分类表
pandas>=2.0.0          # 数据处理
numpy>=1.24.0          # 数值计算
```

---

## 九、后期可扩展

| 功能 | 难度 | 说明 |
|------|------|------|
| 📈 行业历史趋势折线图 | ⭐ | 在侧边栏加个ECharts折线图，选中行业显示近N天趋势 |
| 🔄 行业轮动热力图 | ⭐⭐ | 展示每天排名变化的"行业轮动表" |
| 🌐 北向资金叠加 | ⭐⭐ | 额外拉取北向资金数据，叠加到行业视图上 |
| 📊 导出报告 | ⭐ | 一键导出当天资金流向截图/PDF |
| 🔔 异动预警 | ⭐⭐⭐ | 某行业成交额或涨跌幅超过阈值时，自动推送飞书通知 |
| 🗺️ 日K线联动 | ⭐⭐⭐ | 点击个股可直接跳转K线页 |

---

## 十、方案确认清单

| 事项 | 结论 |
|------|------|
| ✅ 产品名 | 资金云图 |
| ✅ 6个核心指标 | 成交额+涨跌幅+市值变化+流通市值变化+占比+换手率 |
| ✅ 数据源 | baostock(股票列表/指数成分股) + 腾讯qt.gtimg.cn(行情) |
| ✅ 行业分类 | 申万三级，匹配不上标"未分类" |
| ✅ 涨跌幅加权 | 成交额加权 |
| ✅ 板块判断 | 代码前缀：60=沪主板/00=深主板/30=创业板/688=科创板/8x=北交所 |
| ✅ 指数成分股 | baostock动态拉取沪深300/中证500 |
| ✅ 流入流出排序 | 流通市值变化 |
| ✅ 历史数据 | SQLite存储，首次次日自动计算变化值 |
| ✅ 可视化 | Treemap(全程，含个股层级)+侧边排名+底部数据表 |
| ✅ 交互 | 点击下钻、双击返回、日期/板块/指数筛选 |
| ✅ 启动 | python app.py一键启动，有缓存秒开 |
| ✅ 日期选择 | 仅高亮有数据的日期 |
| ✅ 非交易日 | 检测无数据时提示退出 |
| ✅ 停牌股票 | 成交额=0自动跳过 |
| ✅ 端口 | 8080 |
| ✅ 存储 | SQLite (data/history.db) |
| ✅ 首次运行 | 变化值标记null，次日恢复 |

---

_方案版本：v1.0 | 2026-05-29_  
_待确认后进入开发阶段_
